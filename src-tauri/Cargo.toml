[package]
name = "yt-download"
version = "0.1.0"
description = "Local desktop download manager"
authors = ["YT Download"]
edition = "2021"

[lib]
name = "yt_download_lib"
crate-type = ["cdylib", "rlib"]

[[bin]]
name = "yt-download"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
tauri-plugin-shell = "2"

[build-dependencies]
tauri-build = { version = "2", features = [] }
